import { CoordinateType } from "./types";

export const STAGE_HEIGHT = 20;
export const STAGE_WIDTH = 12;
export const START_POS = { x: STAGE_WIDTH / 2 - 2, y: 0 } as CoordinateType;
export const linePoints = [40, 100, 300, 1200];
